{
 "product": "apiant-website",
 "group": "apiant",
 "stamp": "seeded 2026-08-16 · nothing tested yet",
 "running": false,
 "issueBaseUrl": "https://github.com/Fredlumiere/apiant-website/issues/",
 "issuesHeading": "Issues",
 "lastCommit": "ac26bf6079cdf35c5be8bc83e3e0c718e70896d3",
 "areas": [
  {
   "name": "A · Edge functions",
   "tests": [
    [
     "admin-data: serves the right caller and refuses the wrong one",
     "pending",
     "public endpoint. Allow AND deny paths are two different tests"
    ],
    [
     "auth-page: serves the right caller and refuses the wrong one",
     "pending",
     "public endpoint. Allow AND deny paths are two different tests"
    ],
    [
     "blog-get-post: serves the right caller and refuses the wrong one",
     "pending",
     "public endpoint. Allow AND deny paths are two different tests"
    ],
    [
     "blog-list-posts: serves the right caller and refuses the wrong one",
     "pending",
     "public endpoint. Allow AND deny paths are two different tests"
    ],
    [
     "blog-mark-live: serves the right caller and refuses the wrong one",
     "pending",
     "public endpoint. Allow AND deny paths are two different tests"
    ],
    [
     "blog-publish-post: serves the right caller and refuses the wrong one",
     "pending",
     "public endpoint. Allow AND deny paths are two different tests"
    ],
    [
     "blog-save-post: serves the right caller and refuses the wrong one",
     "pending",
     "public endpoint. Allow AND deny paths are two different tests"
    ],
    [
     "blog-upload-media: serves the right caller and refuses the wrong one",
     "pending",
     "public endpoint. Allow AND deny paths are two different tests"
    ],
    [
     "check-availability: serves the right caller and refuses the wrong one",
     "pending",
     "public endpoint. Allow AND deny paths are two different tests"
    ],
    [
     "contact-form: serves the right caller and refuses the wrong one",
     "pending",
     "public endpoint. Allow AND deny paths are two different tests"
    ],
    [
     "get-calendly-url: serves the right caller and refuses the wrong one",
     "pending",
     "public endpoint. Allow AND deny paths are two different tests"
    ],
    [
     "get-lead-context: serves the right caller and refuses the wrong one",
     "pending",
     "public endpoint. Allow AND deny paths are two different tests"
    ],
    [
     "log-interaction: serves the right caller and refuses the wrong one",
     "pending",
     "public endpoint. Allow AND deny paths are two different tests"
    ],
    [
     "save-forum-request: serves the right caller and refuses the wrong one",
     "pending",
     "public endpoint. Allow AND deny paths are two different tests"
    ],
    [
     "send-otp: sends a one-time code to a real address and refuses an unknown or rate-limited one",
     "pending",
     "Named by the scan pass; previously hidden behind the placeholder row \"6 more functions not listed\". Public edge function, so allow and deny are both owed."
    ],
    [
     "submit-apiapps: accepts a valid app submission and refuses a malformed or unauthenticated one",
     "pending",
     "Named by the scan pass; previously hidden behind the placeholder row \"6 more functions not listed\". Public edge function, so allow and deny are both owed."
    ],
    [
     "submit-lead: records a valid lead and refuses a malformed or spammed one",
     "pending",
     "Named by the scan pass; previously hidden behind the placeholder row \"6 more functions not listed\". Public edge function, so allow and deny are both owed."
    ],
    [
     "update-lead-status: lets an authorised caller move a lead and refuses everyone else",
     "pending",
     "Named by the scan pass; previously hidden behind the placeholder row \"6 more functions not listed\". Public edge function, so allow and deny are both owed."
    ],
    [
     "verify-company-type: classifies a real company and refuses an unauthenticated caller",
     "pending",
     "Named by the scan pass; previously hidden behind the placeholder row \"6 more functions not listed\". Public edge function, so allow and deny are both owed."
    ],
    [
     "verify-otp: accepts the code just issued and refuses a wrong, reused or expired one",
     "pending",
     "Named by the scan pass; previously hidden behind the placeholder row \"6 more functions not listed\". Public edge function, so allow and deny are both owed."
    ]
   ]
  }
 ],
 "issues": [],
 "log": [
  [
   "2026-08-16",
   "Board created by /qa-board init. Every row is pending: seeded from what exists in the repo, never from anything that was tested."
  ],
  [
   "2026-08-16",
   "Seeding read: Supabase functions."
  ],
  [
   "2026-08-20 16:16 UTC",
   "Scan pass. The row reading \"6 more functions not listed\" was hiding exactly the six the scan named. A count is not a test row: it cannot pass, fail or carry evidence. Replaced with six named pending rows, so the board's denominator is now honest."
  ]
 ],
 "coverage": {
  "items": [
   [
    "scan",
    "send-otp",
    "covered",
    "A: send-otp, sends a one-time code and refuses an unknown or rate-limited address"
   ],
   [
    "scan",
    "submit-apiapps",
    "covered",
    "A: submit-apiapps, accepts a valid submission and refuses a malformed one"
   ],
   [
    "scan",
    "submit-lead",
    "covered",
    "A: submit-lead, records a valid lead and refuses a malformed one"
   ],
   [
    "scan",
    "update-lead-status",
    "covered",
    "A: update-lead-status, authorised caller moves a lead, everyone else refused"
   ],
   [
    "scan",
    "verify-company-type",
    "covered",
    "A: verify-company-type, classifies a real company, refuses unauthenticated"
   ],
   [
    "scan",
    "verify-otp",
    "covered",
    "A: verify-otp, accepts the code just issued and refuses a wrong or reused one"
   ]
  ],
  "at": "2026-08-20T16:16Z",
  "scannedTo": "0fb056cfa5a07a93877435d8248530433a387e96"
 }
}
